package com.curriculum.labs;

/**
 * Validator Lab — a small utility class we build and exercise from
 * {@code main}, assert-style: call a method, print what we expected next
 * to what we actually got.
 *
 * Follow the README's guided walkthrough. Each part tells you what to add
 * and points at one of the numbered markers below.
 */
public class Validator {

    public static void main(String[] args) {
        System.out.println("=== Validator Lab ===");

        // [MARKER 1] Part 1: pass-by-value — call bumpPrimitive,
        //            bumpFirstElement, and replaceArray here.

        // [MARKER 2] Part 2: varargs — call sum(...) a few different ways.

        // [MARKER 3] Part 3: fail-fast validation — call average(...) and
        //            pauseBriefly() here.

        System.out.println("Done.");
    }

    // [MARKER 4] Part 1: bumpPrimitive(int), bumpFirstElement(int[]), and
    //            replaceArray(int[]) go here.

    // [MARKER 5] Part 2: sum(int... numbers) goes here.

    // [MARKER 6] Part 3: average(int[] numbers) goes here.

    // [MARKER 7] Part 3: pauseBriefly() goes here.
}
