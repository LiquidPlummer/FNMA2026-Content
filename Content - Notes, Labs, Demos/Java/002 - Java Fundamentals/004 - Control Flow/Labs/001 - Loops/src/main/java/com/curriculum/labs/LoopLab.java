package com.curriculum.labs;

import java.util.Scanner;

/**
 * Loop Lab — a small console app we build up piece by piece.
 *
 * Follow the README's guided walkthrough. Each section tells you what to
 * add and points at one of the numbered markers below.
 */
public class LoopLab {

    // Used by the for-each section (Part 4). Don't remove it — we'll get to it.
    static final int[] QUIZ_SCORES = {88, 72, -1, 95, 64, -1, 79, 100};

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        System.out.println("=== Loop Lab ===");

        // [MARKER 2] The menu loop (do-while) will wrap everything below.

        // [MARKER 1] Part 1: the halving loop (while) goes here first.
        //            In Part 2 it becomes menu option 1.

        System.out.println("Goodbye!");
        scanner.close();
    }

    // [MARKER 3] Part 3: countdown(int start) method (for loop) goes here.

    // [MARKER 4] Part 4: averageScores(int[] scores) method (for-each) goes here.
}
