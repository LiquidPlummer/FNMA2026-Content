package com.curriculum.labs;

/**
 * Contract Lab — interfaces and abstract classes, built and compared.
 *
 * Follow the README. The Exporter interface, its implementations, and
 * the TextReport abstract family are all files you'll create.
 */
public class ContractLab {

    public static void main(String[] args) {
        ReportData data = ReportData.sample();

        // [MARKER 1] Part 1: export via CsvExporter.

        // [MARKER 2] Part 2: the deliver(...) call — swap implementations
        //            here with a one-line change.

        // [MARKER 3] Part 4: render the two TextReport subclasses.
    }

    // [MARKER 4] Part 2 adds the deliver(...) method here.
}
